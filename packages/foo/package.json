{
  "private": true,
  "name": "@tslib/foo",
  "version": "0.0.1",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "rslib build --watch",
    "build": "rslib build",
    "test": "vitest run --dir __tests__",
    "clean": "rm -rf dist coverage",
    "prepare": "npm run build",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@tslib/shared": "workspace:*"
  },
  "devDependencies": {
    "@rslib/core": "0.6.7",
    "typescript": "4.9.4"
  }
}