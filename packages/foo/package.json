{
  "private": true,
  "name": "@nomadland/foo",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./lib",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@nomadland/shared": "1.0.0",
    "@nomadland/types": "1.0.0"
  },
  "devDependencies": {
    "rimraf": "4.1.0",
    "typescript": "4.9.4"
  }
}